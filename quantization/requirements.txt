# Python dependencies for PP-OCRv5 INT8 Quantization-Aware Training (QAT)
#
# Step 1 - Data download only (CPU, no GPU needed):
#   pip install datasets Pillow huggingface-hub
#   python3 scripts/download_dataset.py
#
# Step 2 - QAT training (GPU required):
#   pip install paddlepaddle-gpu paddleslim paddle2onnx
#   bash scripts/run_qat.sh

# --- Data preparation (CPU) ---
datasets>=2.14.0
Pillow>=9.0.0
huggingface-hub>=0.20.0

# --- QAT training (GPU) ---
# NOTE: For QAT, replace paddlepaddle with paddlepaddle-gpu:
#   pip install paddlepaddle-gpu
# The CPU version below is only for data prep / validation.
paddlepaddle>=2.6.0

# PaddleSlim provides QAT with PACT activation clipping
paddleslim>=2.6.0

# --- ONNX export ---
paddle2onnx>=1.2.0
