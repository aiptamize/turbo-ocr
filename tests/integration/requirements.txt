# Pinned exact versions (generated from a working venv via `pip freeze`).
# To refresh: bump versions in a venv, run the integration tests green, then `pip freeze`.
pytest==9.0.3
requests==2.33.1
Pillow==12.2.0
reportlab==4.4.10
